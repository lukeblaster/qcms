import { Meta, StoryObj } from '@storybook/react'
import RemoveProductButton from '.'
export default {
  title: 'RemoveProductButton',
  component: RemoveProductButton
} as Meta
export const Default: StoryObj = {}
